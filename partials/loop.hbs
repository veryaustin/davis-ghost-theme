{{#foreach posts}}

<article class="articles">
  <h4 class="article-date" datetime="{{date format="MMMM D DD YYYY" }}">{{date format= "MMMM DD, YYYY"}}</h4>
  <h1 class="article-title"><a href="{{url}}">{{title}}</a></h1>
  {{#if tags}}
  <span class="taglist">
      {{#foreach tags}}
        <a href="{{url}}" title="{{name}}" class="tag">{{name}}</a>
      {{/foreach}}
    {{/if}}
  </span>
</article>

{{/foreach}}
